require 'spec_helper'

describe SongExtender do
  it 'has a version number' do
    expect(SongExtender::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
